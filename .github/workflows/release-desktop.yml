# Desktop installers moved to https://github.com/EXboys/skilllite-assistant
# (.github/workflows/release.yml in that repository).
name: Desktop release (moved)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Point at the desktop repository
        run: |
          echo "SkillLite Assistant is https://github.com/EXboys/skilllite-assistant"
          echo "Run desktop releases there, not in this engine repo."
          exit 1
