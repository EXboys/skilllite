{
  "name": "skilllite-monorepo",
  "private": true,
  "description": "Root package for npm scripts; desktop app lives in skilllite-assistant/",
  "scripts": {
    "assistant:dev": "npm run tauri dev --prefix skilllite-assistant",
    "assistant:build": "npm run tauri:build --prefix skilllite-assistant"
  }
}
